version: '3.9'

services:
  frontend:
    image: karandev1290/baaz-blockscout-frontend-new-1
    pull_policy: always
    platform: linux/amd64
    restart: always
    container_name: 'frontend'
    env_file:
      -  ../envs/common-frontend.env
